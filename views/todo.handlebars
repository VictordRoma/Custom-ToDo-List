<head>
  <link rel="stylesheet" href="/public/css/todo.css">
</head>

<nav class="navbar">
  <div class="container-fluid">
    <a href class="navbar-brand" style="color: #FFFFF1">Tarefas</a>
    <a href="/logout" class="navbar-brand" style="color: #FFFFF1">Sair</a>
  </div>
</nav>

<div class="container">
  <div class="header-container">
    <h1>Minhas Tarefas</h1>
    <button type="button" class="btn btn-primary btnplus" data-bs-toggle="modal"
      data-bs-target="#editarModal"
      onclick="window.location.href='./create'">
      <span class="spn">+</span>
    </button>
  </div>

  {{#each post}}
  {{#dataValues}}
  <div class="container">
    <div class="task-container">
      <div class="task-content">
        <div class="task-details">
          <div class="fw-bold">{{title}}</div>
          <p>{{due}}</p>
        </div>
        <div class="task-buttons">
          <button type="button" class="btn btn-primary btnview"
            data-bs-toggle="modal" data-bs-target="#editarModal">
            <a href="/view/{{id}}" style="color:inherit">Visualizar</a>

          </button>
          <button type="button" class="btn btn-primary btnconcluir"
            data-bs-toggle="modal" data-bs-target="#exampleModal">
            Concluir
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="exampleModal" tabindex="-1"
  aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-body"><p>
          Após concluir esta tarefa, você não poderá consultá-la novamente. Tem
          certeza de que deseja concluí-la?
        </p>

      </div>
      <div class="modal-footer">
        <button type="submit" class="btn btn-primary btnconcluir"
          data-bs-dismiss="modal"><a href="/completar/{{id}}"
            style="color:inherit">Concluir</a></button>
      </div>
    </div>
  </div>
</div>

{{/dataValues}}
{{/each}}

<!-- Modal -->
